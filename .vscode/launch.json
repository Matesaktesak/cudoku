{
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/debug/cudoku",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "CMake: debug"
        }
    ]
}